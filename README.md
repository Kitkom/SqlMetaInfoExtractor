# SqlMetaInfoExtractor

A sql's meta info extractor based on LogicalPlan parsed by spark.

Goal:
  1. extract table-level lineage (done)
  2. extract column-level lineage (done)
  3. extract join-frames (WIP)
